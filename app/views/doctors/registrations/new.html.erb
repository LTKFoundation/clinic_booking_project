<h3>Register new doctor</h3>
<p>Welcome to us! We really need people like you - good doctors to spread the helps to all patient
in Vietnam. Make Vietnam healthy again.</p>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="input-field">
    <%= f.text_field :name, autofocus: true %>
    <%= f.label :name %>
  </div>

  <div class="input-field">
    <%= f.email_field :email %>
    <%= f.label :email %>
  </div>

  <div class="input-field">
    <%= f.password_field :password, autocomplete: "off" %>
    <%= f.label :password %>
  </div>

  <div class="input-field">
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
    <%= f.label :password_confirmation %>
  </div>

  <div class="input-field form-actions">
    <%= f.button "Sign up", class: "waves-effect waves-light btn", type:"submit" %>
  </div>
<% end %>

<%= render "doctors/shared/links" %>
